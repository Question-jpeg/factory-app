

export enum SELECTIONS {
    PIPE,
    CONNECTION,
    SERVO,
    DELETE,
    BUILD,
    PLAY
}

export enum DIRECTIONS {
    TOP,
    RIGHT,
    BOTTOM,
    LEFT
}

export enum EDIT_MODES {
    DELETE,
    CONNECT,
    SERVO,
    NONE
}