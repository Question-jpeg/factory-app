export enum SELECTIONS {
  PIPE,
  CONNECTION,
  SERVO,
  DELETE,
  BUILD,
  PLAY,
  BENCH,
  METAL_FORMER,
}

export enum DIRECTIONS {
  TOP,
  RIGHT,
  BOTTOM,
  LEFT,
}

export enum EDIT_MODES {
  DELETE,
  CONNECT,
  SERVO,
  NONE,
}

export enum METAL_FORMER_MODES {
  ROLLING,
  CUTTING,
  EXTRUDING,
}
